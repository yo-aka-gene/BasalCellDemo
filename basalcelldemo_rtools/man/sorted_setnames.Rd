% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sorting.R
\name{sorted_setnames}
\alias{sorted_setnames}
\title{Create a sorted and deduplicated named vector}
\usage{
sorted_setnames(values, names, sort_by)
}
\arguments{
\item{values}{A vector of values for setNames (e.g., color hex codes).}

\item{names}{A vector of names for setNames.}

\item{sort_by}{A vector used to define the sorting order of the output.}
}
\value{
A named vector where elements are `values` and names are `names`,
  sorted by `sort_by` and deduplicated by `names`.
}
\description{
Extracts unique name-value pairs from vectors, sorted by a reference vector.
This is particularly useful for generating clean, ordered color maps
(named vectors) for `ComplexHeatmap` annotations from a data frame.
}
\examples{
df <- data.frame(
  leiden = c("2", "1", "1", "3", "2"),
  color = c("#00FF00", "#FF0000", "#FF0000", "#0000FF", "#00FF00"),
  celltype = c("B cell", "T cell", "T cell", "NK cell", "B cell")
)
# Create a color map for celltypes, ordered by leiden clusters
sorted_setnames(df$color, df$celltype, df$leiden)
}
